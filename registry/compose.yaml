services:
  registry:
    container_name: registry
    image: registry:3
    ports:
      - 5000:5000
    restart: unless-stopped
    networks:
      docker-net:
        aliases:
          - registry
      default: {}
networks:
  docker-net:
    external: true